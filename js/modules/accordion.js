"use strict";

const accordianHeader = function (dato, sted, type, sum, esn) {
  let htmlText = ``;
  return htmlText;
};

const accordianBody = function (serier) {
  let htmlText = ``;
  return htmlText;
};

const accordianFooter = function () {
  let htmlText = ``;
  return htmlText;
};

export { accordianHeader, accordianBody, accordianFooter };
